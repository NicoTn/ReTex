<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Central palette + shared control styles for ReTex's dark theme. Colours live here so a
        visual tweak is a one-line edit and every window stays consistent. The palette is a
        lightened navy (the old #0B1220 panels read as near-black; these are a clearer blue).
    -->

    <!-- Backgrounds -->
    <SolidColorBrush x:Key="Bg.Window"     Color="#141D30"/>
    <SolidColorBrush x:Key="Bg.Toolbar"    Color="#212D45"/>
    <SolidColorBrush x:Key="Bg.Status"     Color="#141D30"/>
    <SolidColorBrush x:Key="Bg.Panel"      Color="#18233A"/>
    <SolidColorBrush x:Key="Bg.Input"      Color="#1E2A45"/>
    <SolidColorBrush x:Key="Bg.InputHover" Color="#273353"/>
    <SolidColorBrush x:Key="Bg.Selected"   Color="#31466E"/>
    <SolidColorBrush x:Key="Bg.Chip"       Color="#3A465C"/>

    <!-- Lines / borders -->
    <SolidColorBrush x:Key="Border.Muted" Color="#3A465C"/>
    <SolidColorBrush x:Key="Border.Focus" Color="#5B7CC2"/>
    <SolidColorBrush x:Key="Line.Numbers" Color="#5A6B85"/>

    <!-- Foregrounds -->
    <SolidColorBrush x:Key="Fg.Primary" Color="#DAE1ED"/>
    <SolidColorBrush x:Key="Fg.Status"  Color="#9AA6BC"/>
    <SolidColorBrush x:Key="Fg.Muted"   Color="#8593A9"/>

    <!-- Severity accents (status area, badges, indicators) -->
    <SolidColorBrush x:Key="Accent.Ok"    Color="#34D399"/>
    <SolidColorBrush x:Key="Accent.Warn"  Color="#F59E0B"/>
    <SolidColorBrush x:Key="Accent.Error" Color="#F87171"/>

    <!-- Section heading (the "Mods" / "config.cpp" labels). Weight only; the foreground is inherited
         from the window so it tracks the theme without a per-element colour. -->
    <Style x:Key="Heading" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <!-- Small muted caption text -->
    <Style x:Key="Caption" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Fg.Muted}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <!-- ============================ Control styles (implicit) ============================ -->

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource Bg.Input}"/>
        <Setter Property="Foreground" Value="{StaticResource Fg.Primary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border.Muted}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="3,2"/>
        <Setter Property="CaretBrush" Value="{StaticResource Fg.Primary}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource Bg.Selected}"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Fg.Primary}"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Bg.Input}"/>
        <Setter Property="Foreground" Value="{StaticResource Fg.Primary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border.Muted}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="b" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="b" Property="Background" Value="{StaticResource Bg.InputHover}"/>
                            <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Border.Focus}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="b" Property="Background" Value="{StaticResource Bg.Selected}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource Bg.Panel}"/>
        <Setter Property="Foreground" Value="{StaticResource Fg.Primary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border.Muted}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{StaticResource Fg.Primary}"/>
        <Setter Property="Padding" Value="4,3"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="b" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="b" Property="Background" Value="{StaticResource Bg.InputHover}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="b" Property="Background" Value="{StaticResource Bg.Selected}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox: fully templated so the dropdown popup is dark too (the default popup is white). -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource Fg.Primary}"/>
        <Setter Property="Padding" Value="6,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="b" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="b" Property="Background" Value="{StaticResource Bg.Selected}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource Fg.Primary}"/>
        <Setter Property="Background" Value="{StaticResource Bg.Input}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border.Muted}"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Focusable="False" ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="tb" Background="{StaticResource Bg.Input}" BorderBrush="{StaticResource Border.Muted}"
                                            BorderThickness="1" CornerRadius="3" SnapsToDevicePixels="True">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="18"/>
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource Fg.Status}"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="tb" Property="BorderBrush" Value="{StaticResource Border.Focus}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{StaticResource Bg.Panel}" BorderBrush="{StaticResource Border.Muted}" BorderThickness="1" Margin="0,2,0,0">
                                <ScrollViewer>
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Tabs (Texture / 3D Model) -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource Fg.Primary}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="b" Background="{StaticResource Bg.Toolbar}" BorderBrush="{StaticResource Border.Muted}"
                            BorderThickness="1,1,1,0" Margin="0,0,2,0" Padding="12,5">
                        <ContentPresenter ContentSource="Header"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="b" Property="Background" Value="{StaticResource Bg.Panel}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Fg.Status}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource Bg.Panel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border.Muted}"/>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource Border.Muted}"/>
    </Style>

</ResourceDictionary>
